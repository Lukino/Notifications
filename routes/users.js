var express = require('express');
var router = express.Router();

/* GET users listing. */
router.get('/', function(req, res, next) {
  res.send('respo sdnd with a res sdf sdfsdasd as dource');
});

module.exports = router;
